# Handoff — builder

This file carries in-flight context across sessions. Update before
going idle. Read first on every wake-up.

## Currently in flight

REPLACE_ME — what work is active right now? Be specific: what task,
what branch, what's the next concrete step?

## Open questions

REPLACE_ME — what do you need someone else's call on before you can
proceed?

## Don't lose this

REPLACE_ME — context that would be lost if a fresh session started
without it. Examples: a non-obvious gotcha, a decision rationale, a
prior-attempt that failed.

